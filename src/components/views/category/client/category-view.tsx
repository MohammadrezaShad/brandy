import React, {FC, ReactNode} from 'react';

type CategoryViewProps = {
  children?: ReactNode;
};

const CategoryView: FC<CategoryViewProps> = props => <div>CategoryView</div>;

export default CategoryView;
